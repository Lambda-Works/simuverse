name: CI

on:
  push:
    branches: [develop, main]
  pull_request:
    branches: [develop, main]

jobs:
  build-dev:
    name: Build & Verify Dev Environment
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/develop' || github.base_ref == 'develop'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup environment variables
        run: |
          cp .env.example .env
          # CI does not ship real Firebase Admin keys — force JWT fallback.
          {
            echo 'JWT_SECRET=ci-test-jwt-secret-32-chars-min'
            echo 'AUTH_TEST_MODE=jwt'
          } >> .env
          mkdir -p apps/web/.next && chmod 777 apps/web/.next
          mkdir -p apps/api-nest/uploads && chmod 777 apps/api-nest/uploads

      - name: Build and start Docker Compose services
        run: |
          docker compose up -d --build || {
            echo "========== docker compose up failed — api-nest logs =========="
            docker compose logs --tail 200 api-nest || true
            exit 1
          }

      - name: Wait for PostgreSQL to be healthy
        run: |
          echo "Waiting for postgres to be healthy..."
          for i in {1..30}; do
            status=$(docker inspect --format='{{.State.Health.Status}}' simuverse-postgres 2>/dev/null || echo "starting")
            if [ "$status" = "healthy" ]; then
              echo "Postgres is healthy!"
              break
            fi
            echo "  ($i/30) Postgres status: $status"
            sleep 2
          done
          if [ "$status" != "healthy" ]; then
            echo "ERROR: Postgres did not become healthy in time"
            docker compose logs postgres
            exit 1
          fi

      - name: Wait for API to be ready
        run: |
          echo "Waiting up to 3 minutes for API to be ready..."
          for i in {1..36}; do
            if curl -f -s --max-time 5 http://localhost:5002/api/health > /dev/null 2>&1; then
              echo "API is ready!"
              break
            fi
            echo "  ($i/36) API not ready yet, waiting 5s..."
            sleep 5
          done
          if ! curl -f -s --max-time 5 http://localhost:5002/api/health > /dev/null 2>&1; then
            echo "ERROR: API did not become ready in time"
            docker compose logs api-nest
            exit 1
          fi

      - name: Wait for Web to be ready
        run: |
          echo "Waiting up to 3 minutes for Web to be ready..."
          for i in {1..36}; do
            if curl -L -f -s --max-time 5 -o /dev/null http://localhost:8080 2>/dev/null; then
              echo "Web is ready!"
              break
            fi
            echo "  ($i/36) Web not ready yet, waiting 5s..."
            sleep 5
          done
          if ! curl -L -f -s --max-time 5 -o /dev/null http://localhost:8080 2>/dev/null; then
            echo "ERROR: Web did not become ready in time"
            docker compose logs web
            exit 1
          fi

      - name: Verify API is running
        run: |
          echo "Checking API at http://localhost:5002/api/health ..."
          curl -f -s --max-time 30 \
            -o /dev/null \
            -w "API Response: HTTP %{http_code}\n" \
            http://localhost:5002/api/health

      - name: Verify Web is running
        run: |
          echo "Checking Web at http://localhost:8080 ..."
          curl -L -f -s --max-time 30 \
            -o /dev/null \
            -w "Web Response: HTTP %{http_code}\n" \
            http://localhost:8080

      - name: Print logs on failure
        if: failure()
        run: |
          echo "========== DOCKER COMPOSE LOGS =========="
          docker compose logs --tail 100

      - name: Cleanup
        if: always()
        run: docker compose down -v

  build-prod:
    name: Build & Verify Prod Environment
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' || github.base_ref == 'main'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup environment variables
        run: |
          mkdir -p secrets
          touch secrets/firebase-service-account.json
          cat > .env << 'EOF'
          POSTGRES_USER=simuverse
          POSTGRES_PASSWORD=SimuVerse2024secret
          POSTGRES_DB=simuverse
          JWT_SECRET=ci-test-jwt-secret-32-chars-min
          CORS_ORIGIN=http://localhost:3000
          NEXT_PUBLIC_API_URL=http://localhost:5001/api
          NEXT_PUBLIC_SITE_URL=http://localhost:3000
          NEXT_PUBLIC_FIREBASE_API_KEY=ci-test-key
          NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=simuverse-dev.firebaseapp.com
          NEXT_PUBLIC_FIREBASE_PROJECT_ID=simuverse-dev
          NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=simuverse-dev.firebasestorage.app
          NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=123456789
          NEXT_PUBLIC_FIREBASE_APP_ID=1:123456789:web:ci-test
          NEXT_PUBLIC_RECAPTCHA_SITE_KEY=ci-test-site-key
          NEXT_PUBLIC_PWA_ENABLED=true
          NEXT_PUBLIC_DEBUG_LOG=false
          NEXT_PUBLIC_CERTIFICATE_ISSUER_NAME=SimuVerse Academy
          RECAPTCHA_SECRET_KEY=ci-test-recaptcha-secret
          DEEPSEEK_API_KEY=ci-test-key
          DEEPSEEK_MODEL=deepseek-v4-flash
          OPENAI_API_KEY=ci-test-openai-key
          OPENAI_CHAT_MODEL=gpt-4o-mini
          ASSESSMENT_HMAC_SECRET=ci-test-hmac-secret-32-chars-min
          PUERTO_BACKEND_NEST=5001
          PUERTO_FRONTEND=3000
          PUERTO_POSTGRES=5433
          EOF

      - name: Verify Web production build
        run: |
          echo "Building Web with Dockerfile.prod to verify no compile errors..."
          docker build \
            --build-arg NEXT_PUBLIC_API_URL=http://localhost:5001/api \
            --build-arg NEXT_PUBLIC_SITE_URL=http://localhost:3000 \
            --build-arg NEXT_PUBLIC_FIREBASE_API_KEY=ci-test-key \
            --build-arg NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=simuverse-dev.firebaseapp.com \
            --build-arg NEXT_PUBLIC_FIREBASE_PROJECT_ID=simuverse-dev \
            --build-arg NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=simuverse-dev.firebasestorage.app \
            --build-arg NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=123456789 \
            --build-arg NEXT_PUBLIC_FIREBASE_APP_ID=1:123456789:web:ci-test \
            --build-arg NEXT_PUBLIC_RECAPTCHA_SITE_KEY=ci-test-site-key \
            --build-arg NEXT_PUBLIC_PWA_ENABLED=true \
            --build-arg NEXT_PUBLIC_DEBUG_LOG=false \
            --build-arg NEXT_PUBLIC_CERTIFICATE_ISSUER_NAME="SimuVerse Academy" \
            -f apps/web/Dockerfile.prod -t web-build-check .
          echo "✓ Web production build successful"

      - name: Build and start Docker Compose services
        run: |
          docker compose -f docker-compose.prod.yml up -d --build || {
            echo "========== docker compose up failed — seed/api logs =========="
            docker compose -f docker-compose.prod.yml logs --tail 200 seed api-nest || true
            exit 1
          }

      - name: Wait for PostgreSQL to be healthy
        run: |
          echo "Waiting for postgres to be healthy..."
          for i in {1..30}; do
            cid=$(docker compose -f docker-compose.prod.yml ps -q postgres 2>/dev/null || true)
            status=$(docker inspect --format='{{.State.Health.Status}}' "$cid" 2>/dev/null || echo "starting")
            if [ "$status" = "healthy" ]; then
              echo "Postgres is healthy!"
              break
            fi
            echo "  ($i/30) Postgres status: $status"
            sleep 2
          done
          if [ "$status" != "healthy" ]; then
            echo "ERROR: Postgres did not become healthy in time"
            docker compose -f docker-compose.prod.yml logs postgres
            exit 1
          fi

      - name: Wait for seed to complete
        run: |
          echo "Waiting for seed to complete..."
          for i in {1..36}; do
            cid=$(docker compose -f docker-compose.prod.yml ps -q -a seed 2>/dev/null || true)
            if [ -z "$cid" ]; then
              echo "  ($i/36) Seed container not found yet, waiting 5s..."
              sleep 5
              continue
            fi
            code=$(docker inspect --format='{{.State.ExitCode}}' "$cid" 2>/dev/null || echo "-1")
            if [ "$code" = "0" ]; then
              echo "Seed completed successfully!"
              break
            fi
            if [ "$code" != "0" ] && [ "$code" != "-1" ]; then
              echo "ERROR: Seed exited with code $code"
              docker logs "$cid"
              exit 1
            fi
            echo "  ($i/36) Seed still running, waiting 5s..."
            sleep 5
          done
          if [ "$code" != "0" ]; then
            echo "ERROR: Seed did not complete in time"
            cid=$(docker compose -f docker-compose.prod.yml ps -q -a seed 2>/dev/null || true)
            [ -n "$cid" ] && docker logs "$cid"
            exit 1
          fi

      - name: Wait for API to be ready
        run: |
          echo "Waiting up to 3 minutes for API to be ready..."
          for i in {1..36}; do
            if curl -f -s --max-time 5 http://localhost:5001/api/health > /dev/null 2>&1; then
              echo "API is ready!"
              break
            fi
            echo "  ($i/36) API not ready yet, waiting 5s..."
            sleep 5
          done
          if ! curl -f -s --max-time 5 http://localhost:5001/api/health > /dev/null 2>&1; then
            echo "ERROR: API did not become ready in time"
            docker compose -f docker-compose.prod.yml logs api-nest
            exit 1
          fi

      - name: Wait for Web to be ready
        run: |
          echo "Waiting up to 3 minutes for Web to be ready..."
          for i in {1..36}; do
            if curl -L -f -s --max-time 5 -o /dev/null http://localhost:3000 2>/dev/null; then
              echo "Web is ready!"
              break
            fi
            echo "  ($i/36) Web not ready yet, waiting 5s..."
            sleep 5
          done
          if ! curl -L -f -s --max-time 5 -o /dev/null http://localhost:3000 2>/dev/null; then
            echo "ERROR: Web did not become ready in time"
            docker compose -f docker-compose.prod.yml logs web
            exit 1
          fi

      - name: Verify API is running
        run: |
          echo "Checking API at http://localhost:5001/api/health ..."
          curl -f -s --max-time 30 \
            -o /dev/null \
            -w "API Response: HTTP %{http_code}\n" \
            http://localhost:5001/api/health

      - name: Verify Web is running
        run: |
          echo "Checking Web at http://localhost:3000 ..."
          curl -L -f -s --max-time 30 \
            -o /dev/null \
            -w "Web Response: HTTP %{http_code}\n" \
            http://localhost:3000

      - name: Print logs on failure
        if: failure()
        run: |
          echo "========== DOCKER COMPOSE LOGS =========="
          docker compose -f docker-compose.prod.yml logs --tail 100

      - name: Cleanup
        if: always()
        run: docker compose -f docker-compose.prod.yml down -v
